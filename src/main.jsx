import React from 'react'
import ReactDOM from 'react-dom/client' // hay que poner client a partir de react18
import App from './App'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
